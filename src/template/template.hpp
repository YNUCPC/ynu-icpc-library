#pragma once

#include <bits/stdc++.h>
using namespace std;

using ll = long long;
using P = pair<int,int>;
constexpr ll MOD = 1000000007;
const int INF = 1 << 30;
#define REP(i,n) for(int i=0,i_len=(n); i<n; i++)
#define ALL(v) (v).begin(), (v).end()